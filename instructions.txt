Download Wordpress
------------------
curl -O https://wordpress.org/latest.tar.gz
tar -xvzf latest.tar.gz

Create scripts
--------------
chmod +x build.sh
chmod +x download-wordpress.sh

Create assets
-------------
wp-config.php
  (https://api.wordpress.org/secret-key/1.1/salt/)
  Note about backticks.
.ebextensions
  cache.config
  deflate.config


Create RDS Instance
-------------------
https://console.aws.amazon.com/rds
Choose "instances" from left (image).
Click "Launch DB instance". Next.
Choose MySQL. Next.
Choose use case "Production - MySQL".
Enter settings: DB instance identifier, username, password. Next.
Under "Public accessibility" choose "no".

Update Security Group
---------------------
Go to https://console.aws.amazon.com/ec2.
Click on "Security groups" (image)
Choose Group Name rds-launch-wizard
Click Inbound
Click Edit -> Add Rule
Choose MySQL/Aurora, TCP, 3306, rds-launch-wizard

Security
--------
EC2 (Gives SSH access to EC2 instances)
   Create key pair
   Download .pem file and move to ~/.ssh
   chmod 400 ~/.ssh/essycode.pem

eb init
   Choose default region
   Choose application

   add deploy: line to .elasticbeanstalk/config.yml

EBS
---
Update env.config
Create application: Web Server Environment
Create environment:
  Platform PHP
  Environment type: Load balancing, auto scaling (image)
Application version: upload your own
Configuration details: Choose a key pair (image)

Once launched, go to configuration > Instances and add RDS security group.
